# Send a Raven

One page website for a fake raven message service.

This website is an excuse to review transitions, animations, scroll-linked and scroll-triggered animations.

## Installation

- install node and npm
- `npm run build` to build the site for production
- `npm run dev` to spin a dev server locally
- `npm run check` to run biomejs for formatting and linting
